import { MatContainerDirective } from './mat-container.directive';

describe('MatContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new MatContainerDirective();
    expect(directive).toBeTruthy();
  });
});
