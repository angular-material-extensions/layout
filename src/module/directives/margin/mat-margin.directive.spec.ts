import { MatMarginDirective } from './mat-margin.directive';

describe('MatMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new MatMarginDirective();
    expect(directive).toBeTruthy();
  });
});
