import {MatPaddingDirective} from './mat-padding.directive';

describe('MatPaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new MatPaddingDirective();
    expect(directive).toBeTruthy();
  });
});
